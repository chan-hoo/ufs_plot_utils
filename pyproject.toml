[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ufs_plot_utils"
version = "0.1.0"
dependencies = [
    "numpy",
    "xarray",
    "matplotlib",
    "cartopy",
    "pyyaml"
]

[tool.setuptools.packages.find]
where = ["."]
